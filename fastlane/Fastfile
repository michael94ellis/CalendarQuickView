# Generated by DeployMate
default_platform(:ios)

platform :ios do
  before_all do
    # setup_ci if ENV["CI"]
  end

  desc "Run tests"
  lane :test do
    run_tests(scheme: "CalendarQuickView")
  end

  desc "Build and upload to TestFlight"
  lane :beta do
    build_app(scheme: "CalendarQuickView", export_method: "app-store")
    upload_to_testflight(skip_waiting_for_build_processing: true)
  end

  desc "Build App Store IPA"
  lane :release do
    build_app(scheme: "CalendarQuickView", export_method: "app-store")
    upload_to_app_store(submit_for_review: false, automatic_release: false, skip_metadata: true, skip_screenshots: true)
  end
end